node_modules/
.next/

# Secrets — never commit these
.env
.env.*
.env(*)
.env*Shared*
# Variants without leading dot (env.local, env(Shared).txt)
env.local
env(*)
env*Shared*

.vercel/
out/
.vercel

# Python (gpu-service)
gpu-service/.venv/
gpu-service/__pycache__/
gpu-service/chroma_db/
*.pyc
__pycache__/

# OS
.DS_Store

# Regime engine runtime
regime_diag.log

# Stray local copies / archives
Taeheon-Terminal/
