# gpu-service runtime dependencies.
#
# Install PyTorch separately for ROCm — pip's index URL differs from the
# default. Example for ROCm 6.0:
#
#     pip install torch --index-url https://download.pytorch.org/whl/rocm6.0
#
# Then install the rest from this file:
#
#     pip install -r requirements.txt
#
# (CUDA boxes can install torch from the default index. CPU dev laptops can
# install plain `torch` — the code falls back gracefully.)

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
chromadb>=0.4.22
sentence-transformers>=2.7.0
transformers>=4.40
requests>=2.31
tqdm>=4.66
beautifulsoup4>=4.12
lxml>=4.9

# Regime engine (regime_dashboard.py at the repo root). dash itself is
# imported by the script but only the html/dcc primitives are touched at
# import time — we never call app.run() from the wrapper.
yfinance>=0.2.40
fredapi>=0.5.1
hmmlearn>=0.3.0
scipy>=1.10
scikit-learn>=1.3
plotly>=5.18
networkx>=3.0
python-dotenv>=1.0
dash>=2.16
pandas>=2.0
numpy>=1.24
