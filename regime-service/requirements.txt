# regime-service — CPU-only, no GPU/ROCm deps
# PyTorch intentionally excluded: regime_dashboard.py has HAS_LSTM fallback
# so the engine runs HMM-only mode, which is fully functional on free-tier RAM.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5

# Regime engine core
yfinance>=0.2.40
fredapi>=0.5.1
hmmlearn>=0.3.0
scipy>=1.10
scikit-learn>=1.3
plotly>=5.18
networkx>=3.0
python-dotenv>=1.0
dash>=2.16
pandas>=2.0
numpy>=1.24
